<%= form_for(@endereco, html: { class: "form-style-5" }) do |f| %>
  <% if @endereco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@endereco.errors.count, "error") %> prohibited this endereco from being saved:</h2>

      <ul>
      <% @endereco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <fieldset>
    <legend><span class="number">1</span> Contato</legend>

    <div class="field">
      <%= f.label :endereco %><br>
      <%= f.text_field :endereco %>
    </div>
    <div class="field">
      <%= f.label :cidade %><br>
      <%= f.text_field :cidade %>
    </div>
    <div class="field">
      <%= f.label :estado %><br>
      <%= f.text_field :estado %>
    </div>
    <div class="field">
      <%= f.label :contato_id %><br>
      <%= f.text_field :contato_id %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <fieldset>
<% end %>
